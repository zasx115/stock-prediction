# ============================================
# 파일명: main.py
# 설명: 메인 실행 파일 - 페이퍼트레이딩 실행
# 작성일: 2025-01-29
# ============================================
#
# 이 파일이 GitHub Actions에서 매일 자동으로 실행됩니다.
# src/ 폴더의 모듈들을 불러와서 순서대로 실행합니다.
# ============================================

# ----- 라이브러리 불러오기 -----
import sys
from datetime import datetime

# ----- 내가 만든 모듈 불러오기 -----
# (아직 구현 전이라 주석 처리)
# from src.data import get_stock_data
# from src.strategy import predict
# from src.paper_trading import execute_trade
# from src.telegram import send_message
# from src.sheets import save_result

# ----- 설정 불러오기 -----
import config


# ============================================
# 메인 실행 함수
# ============================================

def main():
    """
    메인 실행 함수
    
    실행 순서:
    1. 주식 데이터 수집
    2. 전략으로 예측 (매수/매도/관망)
    3. 가상매매 실행
    4. 구글 시트에 기록
    5. 텔레그램으로 알림
    """
    
    # ----- 시작 로그 -----
    print("=" * 50)
    print(f"주식 예측 시스템 시작")
    print(f"실행 시간: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}")
    print(f"분석 종목: {len(config.STOCK_LIST)}개")
    print(f"사용 전략: {config.STRATEGY_TYPE}")
    print("=" * 50)
    
    # ----- 각 종목별로 실행 -----
    for stock_code in config.STOCK_LIST:
        print(f"\n>>> {stock_code} 분석 중...")
        
        # TODO: 실제 로직 구현 후 주석 해제
        
        # 1단계: 데이터 수집
        # data = get_stock_data(stock_code)
        # print(f"  - 데이터 수집 완료: {len(data)}일치")
        
        # 2단계: 예측
        # signal = predict(data)
        # print(f"  - 예측 신호: {signal}")
        
        # 3단계: 가상매매
        # result = execute_trade(stock_code, signal)
        # print(f"  - 매매 실행: {result}")
        
        # 4단계: 기록 저장
        # save_result(result)
        # print(f"  - 구글시트 저장 완료")
        
        # 5단계: 알림
        # send_message(result)
        # print(f"  - 텔레그램 알림 완료")
        
        # 임시 출력 (나중에 삭제)
        print(f"  - (아직 구현 전입니다)")
    
    # ----- 종료 로그 -----
    print("\n" + "=" * 50)
    print("실행 완료!")
    print("=" * 50)


# ============================================
# 프로그램 시작점
# ============================================

# 이 파일을 직접 실행했을 때만 main() 함수 실행
if __name__ == "__main__":
    main()
```

---

## ✅ 1단계 완료 후 폴더 구조
```
stock-prediction/
│
├── 📁 src/
│   ├── data.py           ✅ (임시)
│   ├── strategy.py       ✅ (임시)
│   ├── backtest.py       ✅ (임시)
│   ├── paper_trading.py  ✅ (임시)
│   ├── telegram.py       ✅ (임시)
│   └── sheets.py         ✅ (임시)
│
├── 📁 .github/workflows/
│   └── daily_run.yml     ✅ (임시)
│
├── config.py             ✅ (완성)
├── main.py               ✅ (완성)
├── requirements.txt      ✅ (완성)
└── README.md             ✅ (GitHub이 자동 생성)
