# ============================================
# 파일명: .github/workflows/daily_run.yml
# 설명: GitHub Actions - 매일 자동 실행 설정
# 작성일: 2025-01-29
# ============================================

# TODO: 나중에 실제 설정으로 교체 예정
name: Daily Stock Prediction

on:
  workflow_dispatch:  # 수동 실행 (테스트용)

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: 임시
        run: echo "나중에 설정 예정"
